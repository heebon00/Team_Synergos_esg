# 10_10_39 · products.html / harness-usage.html 위치 이탈 보고

## 무슨 일이 있었나

`npx serve .`로 로컬 서버를 띄우고 폰트 적용을 확인하던 중,
`http://localhost:3000/products.html`이 **404**를 냈다.
디스크를 확인하니 프로젝트 루트(`C:\Users\PC\Desktop\team00\`)에서
HTML 파일 두 개가 사라져 있었다.

전체 디스크 검색 결과, **삭제된 것이 아니라 상위 폴더로 옮겨져 있었다.**

| 파일 | 원래 위치 | 현재 위치 | 크기 | 최종수정 |
|---|---|---|---|---|
| `products.html` | `team00\products.html` | `C:\Users\PC\Desktop\products.html` | 44,941 B | 2026-08-10 09:59:04 |
| `harness-usage.html` | `team00\harness-usage.html` | `C:\Users\PC\Desktop\harness-usage.html` | 7,550 B | 2026-08-07 15:20:30 |

## 현재 상태

- `team00/` 루트에 두 파일 없음
- `git status` → `harness-usage.html`은 `D`(삭제됨)로 잡힘, `products.html`은 원래 untracked라 git이 모름
- 로컬 서버는 `/`(index.html) 200, `/products.html` 404
- 백그라운드 `npx serve` 프로세스는 **아직 실행 중**(포트 3000)

## 영향 범위 — 데이터 손실 없음

- `Desktop\products.html` 44,941 B = 원본 44,879 B + 62 B.
  내가 09:59에 넣은 `<link rel="stylesheet" href="./assets/fonts/pretendard.css">` 추가분과 일치.
  실제 파일 15~20행을 열어 해당 태그가 들어있는 것을 확인했다. **내용 온전함.**
- `Desktop\harness-usage.html` 7,550 B, 수정시각 8/7 — 원본 그대로. **손상 없음.**
- 휴지통에는 오늘(8/10) 날짜 항목이 없다. 즉 삭제 이벤트가 아니라 **이동**이다.

## 추정 원인 — 확인 안 됨

확정할 수 없다. 가능성만 적는다.

1. 탐색기에서 드래그 중 상위 폴더로 놓였을 가능성 (가장 흔한 패턴)
2. 다른 도구/스크립트가 경로 기준을 `Desktop`으로 잡고 이동시켰을 가능성
3. `.claude/hooks/extract-common.js`가 파일을 옮기는 동작을 하는지 여부 — **미확인**
   (`settings.json`에 등록된 훅은 `check-hardcode.fixed.mjs`(PreToolUse) 하나뿐이고,
   `extract-common.js`는 훅으로 등록돼 있지 않다. 실행 주체 불명.)

내가 이 세션에서 실행한 것은 `Edit`(내용 수정)과 `npx serve`(정적 서버)뿐이고,
둘 다 파일을 이동시키지 않는다. 다만 **내가 원인이 아니라고 단정하지는 못한다.**

## 조치안 (승인 필요)

**A안 — 원위치 복구 (권장)**
`Desktop\products.html`, `Desktop\harness-usage.html`을 `team00\`으로 되돌린다.
현재 파일이 최신본이므로 그대로 옮기면 되고, 덮어쓸 대상이 루트에 없어 충돌도 없다.

**B안 — products.html만 복구**
`harness-usage.html`은 하네스 문서라 급하지 않다면 `products.html`만 먼저 되돌린다.

**C안 — 그대로 두기**
의도적으로 옮긴 것이라면 아무것도 하지 않는다.
단 `harness-usage.html`은 git 추적 파일이므로 커밋 시 삭제로 기록된다.

## 사용자에게 확인할 것

1. 두 파일을 `Desktop\`으로 직접 옮기신 적 있는지? (의도적 이동인지 사고인지)
2. A / B / C 중 어느 쪽으로 갈지
3. 백그라운드 `npx serve`(포트 3000)를 종료할지, 확인용으로 계속 띄워둘지
