# 프로젝트 전역 지침 (GLOBAL_GUIDELINES.md)

이 문서는 `Team_Synergos_esg` 프로젝트의 모든 개발자, 디자이너, 협업 AI가 반드시 준수해야 하는 최상위 개발 및 파일 관리 지침입니다.

---

## 1. 기술 스택 및 환경 (Tech Stack)
- **프론트엔드**: HTML5 + Tailwind CSS v4 + Vanilla JS
  - Tailwind CSS v4의 `@theme` 및 `@layer`를 적극적으로 활용하며 인라인 스타일을 지양합니다.
  - 외부 자바스크립트 라이브러리 도입 시 사전 검토 및 합의가 필요합니다.
- **배포 환경**: GitHub Pages를 이용한 정적 호스팅
- **인코딩 표준**: 모든 소스코드, 문서, 주석은 **`UTF-8`** 방식을 강제합니다. (한글 주석 및 문구 깨짐 방지)

---

## 2. 프로젝트 폴더 구조 (Directory Structure)
프로젝트는 아래의 표준 트리를 따르며 구조를 임의로 변경할 수 없습니다.

```
deploy/
├── README.md                # 배포 프로젝트 설명서 (개요, 실행방법 등)
├── CHANGELOG.md             # 프로젝트 변경 이력 관리
├── .gitignore               # 프로젝트 제외 대상 지정
├── index.html               # 메인 진입 페이지 (HTML5 최소 뼈대)
├── assets/                  # 프로젝트 에셋 및 공용 리소스
│   ├── css/
│   │   └── custom.css       # Tailwind 범위 외부 커스텀 스타일 (최소화 유지)
│   ├── js/
│   │   └── main.js          # 공용 자바스크립트 스크립트 (페이지별 독립 로직은 제외)
│   ├── img/                 # 이미지 자원 (.gitkeep 유지)
│   └── icons/               # SVG 및 시스템 아이콘 (.gitkeep 유지)
├── docs/                    # 설계 및 관리 문서
│   ├── styleguide.md        # 디자인 토큰 및 컴포넌트 스타일 규칙
│   └── content-inventory.md # 콘텐츠 인벤토리 및 담당 표
└── _archive/                # 아카이브 폴더
    └── .gitkeep
```

---

## 3. 파일 및 폴더 명명 규칙 (Naming Conventions)
- **영문 우선 적용**: 한글 및 특수문자(`_`, `-`, `$` 제외)가 포함된 파일명 또는 폴더명은 강제로 **영문으로 변환**하여 저장합니다.
- **분류별 폴더링**: 별도의 지시가 없는 임시/초안 파일은 형식별로 분류하여 `_draft/` 폴더 하위에 저장합니다.
- **에셋 파일 접두사 규칙 (`deploy/assets/img/`, `deploy/assets/icons/`)**:
  - 카테고리 썸네일: `cat-[공간명].[확장자]` (예: `cat-living.png`)
  - 히어로 배너: `hero-[공간명].[확장자]` (예: `hero-bedroom.png`)
  - 제품 이미지: `p-[제품명].[확장자]` (예: `p-bekant.png`)
  - 아이콘: `ic-[아이콘명].svg` (예: `ic-bag.svg`)
  - 로고: `logo-[로고명].svg` (예: `logo-a.svg`)

---

## 4. 문제 발생 시 대응 프로토콜 (Error & Issue Protocol)
- **임의 수정 금지**: 작업 도중 또는 소스코드 내에서 에러, 오류, 설정 충돌 등 문제가 발생할 경우 **즉시 임의 해결(Fix)을 시도하지 않습니다.**
- **보고서 생성**: 문제 발생 시 `docs/` 또는 `deploy/docs/` 폴더 내에 `{DD_hh_mm}.md` 형식(예: `07_11_24.md` - 7일 11시 24분)의 문제 상황 및 보고서 파일을 생성합니다.
- **승인 대기**: 작성된 보고서 내용을 프로젝트 관리자(또는 사용자)에게 보고하고, **명확한 승인이 있을 때에만** 수정 작업을 진행합니다.

---

## 5. 협업 및 AI 에이전트 제어 수칙 (Agent & Collaborative Rules)
- **작업 상태 기록 (`work.md`)**: 에이전트는 주기적으로 메인 작업 로드맵 및 백로그 진행 상황을 [work.md](file:///C:/Users/PC/Desktop/team00/work.md) 파일에 체크하고 성과를 기록해야 합니다.
- **AI 안전 제어 (`AGENTS.md`)**: AI 에이전트의 안전한 수정 작업을 위한 앵커 주석(`<!-- SEGMENT: [영역명] -->`) 및 부분 수정 원칙을 철저히 준수합니다.
- **자동 커밋 및 푸시**: 구조 추가 혹은 승인된 코드 수정 후에는 반드시 변경사항을 스테이징(`git add .`)하고 관련 커밋 메시지를 규격화하여 원격 `main` 브랜치에 푸시합니다.
