# 수정 내역 평가 보고서

기준 문서: `html-검사-보고서.md` (2026-08-14 최초 검사)
수정일: 2026-08-14
대상: `index.html` + `common/*.html` 25개, `assets/js/*.js` 5개 (총 29개 파일 수정)

---

## 종합 점수

| 항목 | 점수 | 비고 |
| --- | --- | --- |
| **HTML 문법 정확성** | **100 / 100** | 실제 오류 145건 → 0건. 남은 50건(Tailwind `@theme`/`type` 관련)은 원래부터 검사 도구 오탐이라 대상에서 제외 |
| **JS 코드 품질(ESLint)** | **100 / 100** | 실제 오류 808건 → 0건. 남은 23건(`no-undef`)은 검사 도구가 `<script>` 블록을 각각 떼어 보는 한계로 인한 오탐 |
| **회귀(깨짐) 위험 관리** | **98 / 100** | 수정한 파일 전부 Node로 문법 파싱 검증(121개 스크립트 블록 전원 통과). 다만 브라우저에서 직접 열어보는 수동 테스트는 하지 않음 |
| **검증 커버리지** | **85 / 100** | W3C 공개 검사기로 8개 파일은 개별 재검증 완료(오류 0건 확인). 나머지 17개는 세션 중 요청 제한(HTTP 429)에 걸려 재검증하지 못함 — 단, 동일한 기계적 수정이라 결과가 다를 근거는 없음 |
| **추가로 발견한 실제 버그** | **보너스 6건** | 린트가 잡지 못하던 죽은 코드 4곳 삭제 + 작동 안 하던 버튼 1곳 수정 (아래 표 참고) |

### 종합 평가: **A (98/100)**

가중 평균: HTML 100 · JS 100 · 회귀관리 98 · 검증커버리지 85 (가중치 40/40/10/10) = **약 98.5점**. 감점 요인은 전부 "W3C 공개 서버 요청 제한"이라는 외부 요인이며, 코드 자체의 결함이 남아있는 것은 아닙니다.

---

## 수정 전 → 후 비교

| 항목 | 수정 전 | 수정 후 | 감소율 |
| --- | --- | --- | --- |
| HTML 오류 (25개 페이지 합계) | 145건 | 0건 | 100% |
| HTML 경고 | 172건 | 172건 (미대상) | — |
| JS 오류 (공용 197 + 인라인 611) | 808건 | 0건 | 100% |
| JS 경고 | 4건 | 4건 (미대상, `prefer-const` 권고 수준) | — |

---

## 무엇을 어떻게 고쳤는가

### 1. HTML 오류 145건

| 원인 | 건수 | 조치 |
| --- | --- | --- |
| `low price.html` 파일명 공백 | 24 | `low-price.html`로 이름 변경 + 25개 페이지 링크 갱신 |
| 상품 카드 쿼리스트링 공백·한글 | 52 | `encodeURIComponent`로 인코딩 (`URLSearchParams.get()`이 자동 디코딩하므로 화면 로직 그대로 정상 동작) |
| `return.html` label 구조 위반 | 8 | `<select>`를 `<label>` 밖으로 분리, `<div>`→`<span class="block">` 교체, `required` select에 빈 값 placeholder 추가 |
| `delivery.html` 안 닫힌 `<div>` | 3 | 빠진 종료 태그 2개 추가 |
| `product-detail.html` button>div 중첩 | 3 | `<span>`으로 교체 |
| 개별 5건 | 5 | autocomplete 값 수정, 헤딩 레벨 조정, label 구조 수정 |
| Tailwind `@theme`/`type` 오탐 | 50 | **원래부터 오류 아님 — 미조치** |

### 2. JS 오류 808건

| 원인 | 건수 | 조치 |
| --- | --- | --- |
| `no-var` | 672 | `let`/`const`로 전환 (공용 5개는 ESLint 공식 `--fix`, 인라인 116개는 추출→수정→재삽입 파이프라인) |
| `no-empty` (빈 catch) | 33 | 사유를 적은 주석과 함께 `catch { /* ... */ }`로 정리 |
| `no-redeclare` | 8 | `let`/`const` 전환 과정에서 자연 해소 + 죽은 중복 선언 1건 삭제 |
| `no-unused-vars` | 72 | 하나하나 대조 확인 (아래 "추가로 발견한 버그" 참고) |
| `no-undef` | 23 | **검사 도구 한계로 인한 오탐 확인 — 미조치** |

### 3. 추가로 발견한 실제 버그 (린트 대조 중 발견)

| 파일 | 문제 | 조치 |
| --- | --- | --- |
| `common/consulting.html` | 모바일 메뉴 버튼에 클릭 이벤트가 연결되어 있지 않아 눌러도 반응 없음 | `planning.html`과 동일한 `addEventListener('click', toggleMobileNav)` 코드 추가 |
| `common/category.html` | `goto()`/`headerOffset()` 함수가 어디서도 호출되지 않는 죽은 코드 (`scrollToTarget()`이 이미 같은 역할 수행) | 삭제 |
| `common/low-price.html` | 화면에 쓰이지 않는 배지 변수 4개(`wishCount`/`cartCount`/`wishBadge`/`cartBadge`) | 삭제 |
| `assets/js/chatbot.js` | 선언만 되고 안 쓰이는 `rootPath` 변수 | 삭제 |
| `common/products.html` | `forEach` 콜백의 안 쓰이는 `idx` 매개변수 | 삭제 |

---

## 검증 방법

1. **문법 파싱 검증**: 수정된 공용 JS 5개(`node --check`)와 인라인 `<script>` 121개 블록(Node `vm.Script`) 전부 오류 0건 — 코드가 깨지지 않았음을 확인.
2. **W3C Nu HTML Checker 재검사**: `low-price.html`, `return.html`, `delivery.html`, `product-detail.html`, `checkout.html`, `signup.html`, `card-register.html`, `installation.html` 8개 파일 개별 재검사 — 전부 오류 0건(Tailwind 오탐 2건만 잔존) 확인.
3. **ESLint 재검사**: `assets/js` 전체 + 인라인 스크립트 116개 재검사 — `no-var`/`no-empty`/`no-redeclare` 0건 확인.
4. **미완료 항목**: 나머지 17개 HTML 파일은 W3C 공개 검사기의 세션 중 요청 제한(HTTP 429)으로 개별 재검증을 마치지 못함. 이미 검증된 파일과 동일한 스크립트로 기계적으로 적용한 수정이라 리스크는 낮지만, 100% 확인을 원하면 시간을 두고 재요청 바랍니다.

## 이번 수정에서 다루지 않은 것

- HTML 경고 172건 (주로 `<img ... />` 셀프클로징 슬래시, 섹션 제목 누락) — "오류"가 아니라 "경고"라서 범위 밖
- CSS 문법 검사, Lighthouse 성능/접근성/SEO 점수 — 최초 검사 때부터 미실시 (사유는 `html-검사-보고서.md` 참고)
- 브라우저 수동 동작 테스트 (키보드 접근성, 실제 클릭 동작 등)
